<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body style="background-color: #0f0f2d; color: white;">

<%- include('partials/header') %>





<div class="container mt-4">
    <h1 class="text-center">Chào mừng đến với Shop Gà Fc Mobile </h1>
    <p class="text-center">Trang web mua bán gói Trong Game FC Mobile.</p>

    <!-- Danh sách sản phẩm -->
    <div class="row mt-4">
        <% products.forEach(product => { %>
            <div class="col-md-4 mb-4">
                <div class="card product-card position-relative">

                    <div class="row g-0 align-items-center">
                        <div class="col-4">
                            <img src="<%= product.image %>" class="img-fluid product-image">
                        </div>
                        <div class="col-8">
                            <div class="card-body">
                                <h5 class="card-title"><%= product.name %></h5>
                                <p class="card-text price"><%= product.price %></p>
								<a href="/product/<%= product.id %>" class="btn btn-primary">Mua ngay</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        <% }); %>
    </div>
</div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
